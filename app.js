// prompt

let firstName = prompt("Ismingizni kiriting:");
let lastName = prompt("Familyangizni kiriting:");
alert(
  `Salom,  ${lastName.charAt().toUpperCase()}${lastName
    .slice(1)
    .toLowerCase()} ${firstName.charAt().toUpperCase()}${firstName
    .slice(1)
    .toLowerCase()} Saytimizga hush kelibsiz!`
);
